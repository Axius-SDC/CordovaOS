# Dependencies for the CordovaOS synthetic data generators (host-side).
# Generation writes app/sdc4/import_data/<domain>/*.xml; loading happens in the
# web container via `manage.py load_all_data`.
cuid2==2.0.1
